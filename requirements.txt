# =====================================================================
# SecureBank — Production requirements
# Python 3.11+ recommended. Pin everything for reproducible builds.
# Install: pip install --no-cache-dir -r requirements.txt
# =====================================================================

# ---------- Core web framework ----------
Flask==3.1.3
Werkzeug==3.1.6
Jinja2==3.1.4
itsdangerous==2.2.0
click==8.1.7
MarkupSafe==2.1.5
blinker==1.8.2                 # Required by Flask signals / Sentry / Talisman

# ---------- WSGI / runtime ----------
gunicorn==22.0.0
gevent==24.2.1                 # Async worker class for gunicorn (-k gevent)
greenlet==3.0.3                # gevent dependency, pin to avoid ABI surprises

# ---------- Configuration ----------
python-dotenv==1.0.0
pydantic==2.9.2                # Typed settings / response models
pydantic-settings==2.5.2

# ---------- HTTP client ----------
requests==2.32.4
urllib3==2.2.3
certifi>=2024.8.30             # Up-to-date CA bundle (security)

# ---------- Database & migrations ----------
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.36
Flask-Migrate==4.0.7           # Alembic-based schema migrations
alembic==1.13.3
psycopg2-binary==2.9.10        # PostgreSQL driver

# ---------- Authentication & crypto ----------
Flask-JWT-Extended==4.7.1
flask-bcrypt==1.0.1
bcrypt==4.2.0
PyJWT==2.9.0
cryptography==43.0.3           # TLS / JWT RS256 / secrets

# ---------- Security hardening ----------
Flask-Talisman==1.1.0          # CSP, HSTS, security headers
Flask-Cors==5.0.0              # Strict CORS allow-list
Flask-WTF==1.2.1               # CSRF protection for any form endpoints
bleach==6.1.0                  # HTML sanitization (defense in depth)

# ---------- Rate limiting ----------
Flask-Limiter==3.12.0
limits[redis]==3.13.0          # Redis backend for distributed rate limiting

# ---------- Validation ----------
marshmallow==3.26.1
email-validator==2.2.0

# ---------- Caching / queues / Redis ----------
redis==5.3.0
hiredis==3.0.0                 # C parser, big perf win for redis-py

# ---------- Observability: metrics, tracing, logging ----------
prometheus-flask-exporter>=0.23.1
prometheus-client==0.21.0
sentry-sdk[flask]==2.17.0      # Error tracking + perf monitoring
structlog==24.4.0              # Structured JSON logs
python-json-logger==2.0.7      # JSON formatter for stdlib logging

# ---------- Reliability ----------
tenacity==9.0.0                # Retries with backoff (DB / external calls)

# ---------- Production hygiene ----------
setuptools>=75.1.0             # Avoid known CVEs in old setuptools
pip>=24.2                      # (informational; upgrade in Dockerfile)
